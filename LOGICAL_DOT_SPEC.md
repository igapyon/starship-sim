# Logical Dot Rendering v1 仕様

## 目的

Starship Sim の描画を「論理ドット」ベースで扱い、端末解像度が異なってもゲームロジックと表示の基準を安定させる。

## v1 の確定仕様（実装同期済み）

- 論理空間（world）は `720 x 720` の正方形で固定する
- 戦場境界も `720 x 720` に統一する
- 描画はレターボックス方式（中央寄せ、アスペクト比維持）とする
- ズームはスライダーではなく、`- / % / +` のステッパーUIで操作する
- ズーム段階は固定ステップ:
  - `0.4x, 0.5x, 0.625x, 0.75x, 0.875x, 1x, 1.25x, 1.5x, 1.75x, 2x, 3x, 4x`
- 表示ラベルは `100%` のようにパーセント表示する
- 画面サイズに基づいて、利用可能なズーム段階を動的に再計算する
- `canvas.imageSmoothingEnabled = false` で補間によるにじみを抑える
- マウス/タッチ入力は screen 座標から world 座標へ逆変換して処理する
- world 外（レターボックス余白含む）の入力は無効化する

## 補足方針

- 物理・AI・当たり判定・索敵距離などのゲームロジックは world 座標系で固定する
- UI（シーン選択、ビーコン、ズーム、情報パネル）は world 正方形の周囲余白に寄せて配置する
- 小画面端末でも操作可能性を優先し、ズーム段階は連続値ではなく離散値のみ扱う

## v1 非対象

- ドラッグパン
- ピンチ操作などの連続ズーム
