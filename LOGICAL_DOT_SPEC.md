# Logical Dot Rendering v1 仕様

## 目的

Starship Sim の描画を「論理ドット」ベースに移行し、表示サイズを自由に変えてもにじみを抑えた表示を実現する。

## v1 の確定仕様

- 論理空間（world）は `720 x 720` の正方形で固定する
- 戦場境界も `720 x 720` に統一する
- 表示はレターボックス方式（中央寄せ、アスペクト比維持）とする
- ズームは UI スライダーで操作する
- ズーム段階は固定ステップとし、`0.5x, 0.625x, 0.75x, 0.875x, 1x, 1.25x, 1.5x, 1.75x, 2x, 3x, 4x` を基本候補にする
- 実際に選択可能な段階は、画面サイズに収まる倍率のみ表示する
- `canvas.imageSmoothingEnabled = false` とし、補間によるにじみを無効化する
- マウス/タッチ入力は screen 座標から world 座標へ逆変換して処理する
- レターボックス余白領域の入力は無効とする

## 補足方針

- `1x, 2x, 3x...` が画質優先の標準段階
- `0.625x, 0.75x, 0.875x, 1.25x, 1.5x, 1.75x` は `720` 基準で使いやすい中間段階
- 物理・AI・当たり判定・索敵距離などのゲームロジックは world 座標系で固定する

## v1 非対象

- カメラ追従
- ドラッグパン
- 連続ズーム（段階値以外）
