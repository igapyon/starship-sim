# 画面構成ガイド

このドキュメントは、現在の Starship Sim の画面レイアウトとUI挙動を説明します。

## 全体構成

- 背景全体: フルスクリーン `canvas`
- 戦場: `720 x 720` の正方形 world をレターボックス表示
- UI: `z-index: 10` でキャンバス前面に表示

## レイアウト方針（重要）

UIは固定四隅ではなく、**正方形戦場にできるだけ寄せる**配置ルールです。  
ただし余白不足時は画面端へフォールバックします。

- 左上系: シーン選択ボタン群（`L1D2C8` など）
- 右上系: Beacon選択（A/B/C）+ 残骸トグル
- 右下系: ZOOMステッパー
- 左下系: 情報パネル（チーム状況）

## メインキャンバス

- 背景色: `#001a33`（戦場）
- 表示要素:
  - 3チーム艦船（水色A / 桃色B / 黄色C）
  - 弾・爆発パーティクル
  - 検出ビーコン
  - 索敵円、残骸表示
- 入力:
  - クリック/タップでビーコン配置
  - 戦場外（レターボックス余白）は無効

## UI要素

### 1. シーン選択ボタン（左上系）

- 要素: `#test-buttons`
- 形式: 縦並びボタン
- 内容:
  - `L1D2C8`
  - `C8vsD2`
  - `L1vsD2`
  - `Mass D1C8`
  - `L1D4C16`

### 2. Beacon + 残骸（右上系）

- 要素: `#bottom-controls` 内 `#action-controls`
- Beacon選択:
  - `A / B / C`（SVGアイコン付き）
  - 選択中は `active` ハイライト
- 残骸トグル:
  - `残骸を無視する` / `残骸を攻撃する`

### 3. ZOOM（右下系）

- 要素: `#zoom-controls`
- 形式: ステッパー（スライダーは廃止）
  - 左: 縮小ボタン（SVG）
  - 中央: 現在倍率（例: `100%`）
  - 右: 拡大ボタン（SVG）
- 仕様:
  - 段階ズーム (`ZOOM_STEPS`) を1段ずつ移動
  - 最小/最大では該当ボタンを自動で無効化

### 4. 情報パネル（左下系）

- 要素: `#info`
- 内容:
  - チームA/B/Cの艦数
  - コスト残量
- 表示:
  - 小型フォント
  - 半透明背景

## 操作まとめ

1. シーンボタンでシミュレーション初期化
2. Beacon A/B/C を選択
3. 戦場をクリック/タップしてビーコン配置
4. 必要なら残骸トグルでAI挙動を切り替え
5. ZOOMステッパーで倍率変更

## モバイル対応

- `viewport-fit=cover` 対応
- タッチ操作対応（`touchstart` / `touchmove`）
- テキスト選択無効化 (`user-select: none`)

## 関連ファイル

- テンプレート: `index-src.html`
- スタイル: `src/css/app.css`
- レイアウト/入力/ズーム: `src/ts/main.ts`
- シーン定義: `src/ts/scenes.ts`
- 追従モード仕様: [FOLLOW_MODE_SPEC.md](FOLLOW_MODE_SPEC.md)
