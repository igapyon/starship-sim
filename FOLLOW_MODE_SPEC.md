# 追従モード仕様メモ

最終更新: 2026-02-11

## 目的

スマホなどで画面が小さい場合に、縮小表示だけでなく「見切れを許容して見やすくする」挙動を整理する。

## 確定仕様

1. ズーム初期値は現状維持（デバイス画面サイズ依存の現在ロジックをそのまま使う）。
2. ズームUIは現状の `+/-` ボタンを継続利用する。
3. ワールド全体が画面に収まらないときのみ、追従モードを有効化する。
4. 船の選択はクリック/タップで行う。
5. 追従モード中は、選択中の船を画面中心に常時表示する。
6. 選択中の船が撃沈した場合は、3秒待機してから生存船リスト先頭へ自動フォーカスする（決定的挙動）。
7. Beacon選択UI（A/B/C）はトグル挙動に変更する（各ボタン ON/OFF）。
8. A/B/C がすべて OFF のときは「船追尾モード」とする（ビーコン配置なし）。
9. この挙動はスマホ/PCで共通とする。

## 補足

- 「全体が収まらない」の判定は、現在の表示スケールと画面サイズから機械的に判定する。
- 生存船が存在しない場合の自動フォーカスは行わない（追従対象なし）。
