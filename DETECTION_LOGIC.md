# 索敇（Detection）ロジック

このドキュメントは、Starship Sim における艦船の索敇・目標選択ロジックを説明するものです。

---

## 概要

艦船の行動は以下の段階で決定されます：

1. **目標選択** - どのユニットを攻撃対象にするか決定
2. **索敇範囲判定** - 目標が索敇範囲内にあるか確認
3. **エンジン制御** - 索敇に応じてエンジンの起動・停止を制御

---

## シミュレーション方式

このシステムは **ハイブリッドシミュレーション** です。

**チームA（プレイヤー艦隊）**：
- 索敇範囲内に敵あり → 自動ロジック制御
- 索敇範囲内に敵なし → マウスでの手動操作が可能
- 射撃判定：範囲内の敵が確認されている場合のみ

**チームB（対抗艦隊）**：
- 常に決定論的ロジックで自動制御

※ 開発中期までの過渡的な実装です。最終的には両チームとも完全自動ロジック制御のシミュレーション化を予定しています。

---

## 目標選択ロジック

**実装位置**:
- プレイヤー（チームA）: index.html 1410-1434行
- 敵（チームB）: index.html 1437-1469行

### 共通の優先順位（チームA・チームB共通）

1. **通常状態の目標を探す**
   - 索敇範囲内で weapons.length > 0 の相手を探す
   - 最も近い相手を目標に選択

2. **通常状態の目標が見つからない場合、残骸を探す**
   - 「残骸を無視する」設定（`attackWrecks = false`）の場合：
     - 残骸は探索対象から完全に除外 → 目標なし
   - 「残骸を攻撃する」設定（`attackWrecks = true`）の場合：
     - 索敇範囲内で weapons.length === 0 の相手残骸を探す
     - 最も近い残骸を目標に選択

3. **どちらも見つからない場合**
   - **チームA（プレイヤー）**: マウスカーソル位置を追従（手動操作）
   - **チームB（対抗）**: 停止状態に設定（速度を緩やかに減速）

### 処理フロー（チームA・チームB共通）

```
if (自分が射撃可能) {
  相手を索敇範囲内で探索
  ├─ 通常状態を記録 (closestActive)
  └─ 残骸を記録 (closestDebris)
      ただし attackWrecks = false の場合は記録しない

  目標の優先順位判定
  ├─ closestActive が見つかった → 目標に選択
  └─ closestActive がない場合
      attackWrecks = true なら closestDebris を目標に選択
      attackWrecks = false なら目標なし
}
```

**目標がない場合の動作**：
- チームA（プレイヤー）：マウス位置を追従（手動操作が可能）
- チームB（対抗）：停止状態に設定（速度を緩やかに減速）

---

## チームAとチームBの違い

| 項目 | チームA（プレイヤー） | チームB（対抗） |
|------|-------------------|-----------|
| **通常状態の探索** | 目標選択時に索敇範囲内の敵を探索 | 目標選択時に索敇範囲内の敵を探索 |
| **残骸の探索** | `attackWrecks` 設定に依存 | `attackWrecks` 設定に依存 |
| **目標がない場合** | マウス位置を追従（手動操作） | 停止状態に設定（自動制御） |
| **射撃判定** | 範囲内に目標確認時のみ射撃 | 常に自動判定 |
| **実装位置** | 1410-1434行 | 1437-1469行 |

### 両チーム共通：目標がない場合の動作

```javascript
if (target) {
  ship.update(target.x, target.y);
} else {
  // 停止状態に設定
  ship.thrustersActive = false;
  ship.vx *= 0.95;  // 速度を緩やかに減速
  ship.vy *= 0.95;
  ship.targetVx = 0;
  ship.targetVy = 0;
}
```

---

## 索敇範囲判定と追跡（両チーム共通）

**実装位置**: index.html 428-466行 (update メソッド)

### 索敇範囲チェック

各艦船が update メソッドで実行：

```javascript
if (this.weapons.length > 0) {
  const distance = Math.hypot(targetX - this.x, targetY - this.y);

  if (distance < this.weapons[0].detectionRange) {
    // 索敇範囲内
    this.detectedTarget = true;
    this.thrustersActive = true;
    // 目標位置を設定して追跡開始
  } else {
    // 索敇範囲外
    this.detectedTarget = false;
    this.thrustersActive = false;
  }
}
```

### 重要な特性

- 艦船に weapons がない場合（残骸状態）、索敇は実行されない
- targetX, targetY が渡される座標で判定される
- 索敇範囲は `weapons[0].detectionRange` で定義

---

## 「残骸を攻撃する/無視する」設定の影響

### attackWrecks = false（「残骸を無視する」）

**両チーム共通の行動**:
1. 目標選択時に通常状態の相手のみを探索
2. 相手残骸は探索対象から完全に除外
3. 通常状態の相手がない場合：停止状態に設定

**弾の当たり判定**:
- チームAの弾：常にチームBのすべての艦に当たり判定あり（設定に関わらず）
- チームBの弾：常にチームAのすべての艦に当たり判定あり（設定に関わらず）

### attackWrecks = true（「残骸を攻撃する」）

**両チーム共通の行動**:
1. 目標選択時に通常状態の相手を優先的に探索
2. 通常状態の相手がない場合、相手残骸を探索
3. 通常状態の相手を見つけた場合、それを追跡

**弾の当たり判定**:
- 同上（設定に関わらず常に有効）

---

## 現在の設定ファイル参照

- **UI ボタン**: `#wreckage-toggle`
- **グローバル変数**: `attackWrecks` (デフォルト: false)
- **表示テキスト**:
  - attackWrecks = false: 「残骸を無視する」
  - attackWrecks = true: 「残骸を攻撃する」

---

## 修正履歴

- **v1.0** (2026-02-03): 索敇ロジック統一後のドキュメント作成
  - チームAとチームBの目標選択ロジックを統一
  - `attackWrecks` 設定を両チームに適用

- **v1.1** (2026-02-03): 完全自動シミュレーション化
  - プレイヤー艦をマウス操作型からロジック自動制御型へ変更
  - チームAとチームBが完全に同じロジックで動作するよう統一
  - 「敵」「味方」という表現を廃し、「チームA」「チームB」で統一
  - 「AI」ではなく「決定論的ロジック」で動作する仕組みに変更
