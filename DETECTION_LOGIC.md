# 索敇（Detection）ロジック

このドキュメントは、Starship Sim における艦船の索敇・目標選択ロジックを説明するものです。

---

## 概要

艦船の行動は以下の段階で決定されます：

1. **目標選択** - どのユニットを攻撃対象にするか決定
2. **索敇範囲判定** - 目標が索敇範囲内にあるか確認
3. **エンジン制御** - 索敇に応じてエンジンの起動・停止を制御

---

## シミュレーション方式

このシステムは **3チーム決定論的ロジックシミュレーション** です。

**チームA（艦隊A）**：
- 常に決定論的ロジックで自動制御
- 目標：チームB と チームC の両方を敵と見なす

**チームB（艦隊B）**：
- 常に決定論的ロジックで自動制御
- 目標：チームA と チームC の両方を敵と見なす

**チームC（艦隊C）**：
- 常に決定論的ロジックで自動制御
- 目標：チームA と チームB の両方を敵と見なす

※ すべてのチームは決定論的ロジック制御で動作しますが、特定のテストシーンでは `mouseControlTeam` 変数を使用して特定のチームをマウス操作対象に設定することが可能です。

---

## 目標選択ロジック

**実装位置**:
- チームA: index.html メインループ
- チームB: index.html メインループ
- チームC: index.html メインループ

### 共通の優先順位（全チーム共通）

1. **通常状態の目標を探す**
   - 索敇範囲内で weapons.length > 0 の相手を探す
   - 最も近い相手を目標に選択

2. **通常状態の目標が見つからない場合、残骸を探す**
   - 「残骸を無視する」設定（`attackWrecks = false`）の場合：
     - 残骸は探索対象から完全に除外 → 目標なし
   - 「残骸を攻撃する」設定（`attackWrecks = true`）の場合：
     - 索敇範囲内で weapons.length === 0 の相手残骸を探す
     - 最も近い残骸を目標に選択

3. **どちらも見つからない場合**
   - **全チーム（A・B・C）**: 停止状態に設定（速度を緩やかに減速）

### 処理フロー（チームA・チームB共通）

```
if (自分が射撃可能) {
  相手を索敇範囲内で探索
  ├─ 通常状態を記録 (closestActive)
  └─ 残骸を記録 (closestDebris)
      ただし attackWrecks = false の場合は記録しない

  目標の優先順位判定
  ├─ closestActive が見つかった → 目標に選択
  └─ closestActive がない場合
      attackWrecks = true なら closestDebris を目標に選択
      attackWrecks = false なら目標なし
}
```

**目標がない場合の動作**：
- **マウス操作対象チーム**：マウス位置を追従（`mouseControlTeam` 変数で指定）
- **その他のチーム**：停止状態に設定（速度を緩やかに減速）

---

## 3チーム体制

### 各チームの関係図

```
チームA ←→ チームB
  ↓   ↑     ↓   ↑
  └─────────────┘
  チームC（中央）
```

**競合関係**:
- チームA はチームB とチームC を敵と見なす
- チームB はチームA とチームC を敵と見なす
- チームC はチームA とチームB を敵と見なす

### 全チーム共通：目標がない場合の動作（ビーコンシステム統合）

```javascript
if (target) {
  ship.update(target.x, target.y);
} else {
  // 全チーム共通：停止状態に設定
  ship.thrustersActive = false;
  ship.vx *= 0.95;  // 速度を緩やかに減速
  ship.vy *= 0.95;
  ship.targetVx = 0;
  ship.targetVy = 0;
  // 位置更新（摩擦・画面端反射）
}
```

### 検出ビーコンによるチーム活性化

**ビーコンの機能**:
- マウスクリックで検出ビーコンを配置（チーム選択可能）
- 検出範囲: 180ピクセル（船種D相当）
- 持続時間: 180フレーム（約3秒）
- チーム内の船がビーコン内に入ると即座に活性化

**活性化ロジック**:
```javascript
const inBeacon = isShipInFriendlyBeacon(ship, teamId);
if (inBeacon && ship.weapons.length > 0) {
  ship.thrustersActive = true;
  ship.detectedTarget = true;
  // 敵を探す
}
```

**マウス操作対象チームについて**:
- `mouseControlTeam` 変数はシーン選択用の名前表示に使用
- 全チーム共通でビーコンシステムで活性化
- マウス位置への自動追従は廃止

---

## 索敇範囲判定と追跡（両チーム共通）

**実装位置**: index.html 428-466行 (update メソッド)

### 索敇範囲チェック

各艦船が update メソッドで実行：

```javascript
if (this.weapons.length > 0) {
  const distance = Math.hypot(targetX - this.x, targetY - this.y);

  if (distance < this.weapons[0].detectionRange) {
    // 索敇範囲内
    this.detectedTarget = true;
    this.thrustersActive = true;
    // 目標位置を設定して追跡開始
  } else {
    // 索敇範囲外
    this.detectedTarget = false;
    this.thrustersActive = false;
  }
}
```

### 重要な特性

- 艦船に weapons がない場合（残骸状態）、索敇は実行されない
- targetX, targetY が渡される座標で判定される
- 索敇範囲は `weapons[0].detectionRange` で定義

---

## 「残骸を攻撃する/無視する」設定の影響

### attackWrecks = false（「残骸を無視する」）

**両チーム共通の行動**:
1. 目標選択時に通常状態の相手のみを探索
2. 相手残骸は探索対象から完全に除外
3. 通常状態の相手がない場合：停止状態に設定

**弾の当たり判定**:
- チームAの弾：チームB と チームC のすべての艦に当たり判定あり（設定に関わらず）
- チームBの弾：チームA と チームC のすべての艦に当たり判定あり（設定に関わらず）
- チームCの弾：チームA と チームB のすべての艦に当たり判定あり（設定に関わらず）

### attackWrecks = true（「残骸を攻撃する」）

**両チーム共通の行動**:
1. 目標選択時に通常状態の相手を優先的に探索
2. 通常状態の相手がない場合、相手残骸を探索
3. 通常状態の相手を見つけた場合、それを追跡

**弾の当たり判定**:
- 同上（設定に関わらず常に有効）

---

## 現在の設定ファイル参照

- **UI ボタン**: `#wreckage-toggle`
- **グローバル変数**: `attackWrecks` (デフォルト: false)
- **表示テキスト**:
  - attackWrecks = false: 「残骸を無視する」
  - attackWrecks = true: 「残骸を攻撃する」

---

## 修正履歴

- **v1.0** (2026-02-03): 索敇ロジック統一後のドキュメント作成
  - チームAとチームBの目標選択ロジックを統一
  - `attackWrecks` 設定を両チームに適用

- **v1.1** (2026-02-03): 完全自動シミュレーション化
  - プレイヤー艦をマウス操作型からロジック自動制御型へ変更
  - チームAとチームBが完全に同じロジックで動作するよう統一
  - 「敵」「味方」という表現を廃し、「チームA」「チームB」で統一
  - 「AI」ではなく「決定論的ロジック」で動作する仕組みに変更

- **v2.0** (2026-02-03): チームC 実装
  - 第3勢力チームC（黄色）を追加
  - 3チーム競合体制へ変更（各チームが他2チームを敵と見なす）
  - 弾の当たり判定を3チーム対応に更新
  - すべてのチームが完全自動ロジック制御で動作するよう統一

- **v2.1** (2026-02-03): マウス操作対象チームの柔軟化
  - `mouseControlTeam` グローバル変数を導入
  - シーンごとにマウス操作対象チームを切り替え可能に
  - Team-B D1C8シーン実装（チームBをマウス操作対象に設定）
  - `skipDetectionCheck` パラメータにより、マウス操作時に索敇範囲外でも追従可能

- **v2.2** (2026-02-03): 検出ビーコンシステム実装
  - マウス位置への自動追従機能を廃止
  - 検出ビーコンシステムを実装（クリック配置、チーム別設定）
  - 全チーム統一：ビーコン内で自動活性化
  - 目標マーカー円の削除
