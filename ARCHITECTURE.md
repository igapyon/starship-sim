# アーキテクチャ設計方針

## 単一HTMLファイルという絶対的な魅力

このプロジェクトの**最大の価値は、単一HTMLファイルで完全に動作すること**です。

### なぜこれが最高の強み

1. **即座に動作**
   - ダウンロード → ブラウザで開く → 動作開始
   - npm install、ビルド、サーバー起動なし
   - 環境構築という概念がない

2. **配布と共有が最高にシンプル**
   - ファイル1つをメールやGitHubで送るだけ
   - 誰でも迷わずに実行できる
   - バージョン管理も1ファイル

3. **学習教材として完璧**
   - ブラウザの開発者ツール（DevTools）でコード全体を即座に参照可能
   - 物理エンジン、AI、衝突判定、すべてが1ファイルに凝縮
   - 実装の全貌を素早く理解できる

4. **実験的プロジェクトに最適**
   - 変更 → 保存 → リロード という高速フィードバックループ
   - テストプロジェクトとしての身軽さ
   - アイデア検証に最短時間でたどり着ける

5. **依存関係ゼロ**
   - インターネット接続不要（オフライン動作可能）
   - npm のダウンロード時間がない
   - 将来の依存関係解決の問題がない

### 今後も守るべき原則

このプロジェクトが続く限り、**単一HTMLファイル体制を維持する**ことが最優先です。

- ファイル分割は検討しない
- 外部ライブラリは導入しない
- ビルドツールも不要

この「1ファイル」という制約こそが、他のゲームプロジェクトにない、Starship Sim の唯一無二の価値です。

---

## 概要

このドキュメントでは、Starship Sim プロジェクトにおける設計判断の根拠と、今後の方針について記述します。

## ライブラリ非使用の方針

### なぜ外部ライブラリを使わないのか

このプロジェクトは、以下の理由から**純粋な JavaScript（vanilla JS）のみで実装**する方針を採用しています：

1. **実験的プロジェクトの性質**
   - 物理エンジンとAI戦闘ロジックの実験が主目的
   - 外部依存を排除することで、純粋に実装ロジックに集中できる
   - 学習・研究用途に最適化

2. **配布・共有の容易さ**
   - 単一HTMLファイルで完結
   - ブラウザで開くだけで動作
   - npm install や ビルドプロセスが不要
   - 誰でも即座に実行・改変が可能

3. **透明性と理解しやすさ**
   - すべてのコードが可視化されている
   - ブラックボックスがない
   - 初学者でもコード全体を追跡可能
   - デバッグが容易

4. **パフォーマンス**
   - フレームワークのオーバーヘッドがない
   - 必要な機能だけを実装
   - 軽量で高速な動作

5. **教育的価値**
   - ゲームループ、物理演算、衝突判定などの基礎を学べる
   - フレームワーク依存の知識ではなく、本質的な理解が得られる

## 現在のコード品質

### 洗練された構造

現在のコードは「ベッタリ」（monolithic）ではなく、すでに**良好な構造**を持っています：

#### クラスベース設計

```
- Hull（標準船体）
- LargeHull（大型船体）
- ThrusterEngine（推進エンジン）
- WeaponUnit（標準射撃ユニット）
- AdvancedRadar（高性能レーダー）
- Bullet（弾丸）
- Particle（パーティクルエフェクト）
- Starship（艦船：上記コンポーネントを組み合わせる）
```

#### 設計パターンの実装

1. **コンポーネントパターン**
   - Starshipが船体・エンジン・武器・レーダーを組み合わせる
   - 柔軟な構成が可能（船種C、船種Dなど）

2. **継承の活用**
   - LargeHullがHullを継承
   - 適切なオブジェクト指向設計

3. **責任の分離**
   - 各クラスが明確な役割を持つ
   - Hull: 耐久性管理
   - Engine: 推進力・旋回性能
   - WeaponUnit: 射撃・追尾
   - Starship: コンポーネント統合・全体制御

4. **カプセル化**
   - 各クラスが内部状態を適切に管理
   - public メソッドによるインターフェース提供

### コード量の妥当性

- 総行数: 約1,550行
- 単一ファイルとして管理可能な範囲
- 複雑すぎず、シンプルすぎない適切なバランス

## 現状維持の判断理由

### リファクタリングを行わない理由

以下の理由から、**大規模なリファクタリングは不要**と判断しました：

#### 1. 現在の価値を損なうリスク

- 単一ファイルのシンプルさが最大の強み
- ファイル分割により、コードの見通しが悪化する可能性
- 実験的プロジェクトとしての手軽さが失われる

#### 2. 過剰設計のリスク

- 1,550行のコードに対して、エンジン層の分離は重すぎる
- YAGNI原則（You Aren't Gonna Need It）に反する
- メンテナンスコストが増加

#### 3. 学習コストの増加

- 複数ファイルを行き来する必要が生じる
- 初学者が全体像を把握しにくくなる
- コードリーディングの難易度が上がる

#### 4. プロジェクトの目的との不整合

- このプロジェクトは「アーキテクチャの美しさ」ではなく「物理とAIの実験」が目的
- 設計パターンの実践場ではない
- シンプルさこそが本質的価値

## 今後の方針

### 現状維持を推奨する条件

以下の状態が続く限り、現在のアーキテクチャを維持します：

- コード量が3,000行未満
- 単独開発または少人数開発
- 実験・学習目的の範囲内
- 単一HTMLファイルでの配布が望ましい

### リファクタリングを検討すべき条件

以下のいずれかに該当する場合、設計の見直しを検討します：

1. **規模の拡大**
   - コード量が3,000行を超える
   - 機能が大幅に増加し、1ファイルでの管理が困難

2. **開発体制の変化**
   - 複数人での開発が必要になった
   - チーム開発の効率化が求められる

3. **再利用の必要性**
   - 他プロジェクトでエンジン部分を再利用したい
   - ライブラリとして切り出す価値が生まれた

4. **テストの必要性**
   - 自動テストが必須になった
   - CI/CDパイプラインの構築が必要

5. **プロジェクトの性質変化**
   - 実験から本格的なゲーム開発へ移行
   - プロダクションレベルの品質が求められる

## 将来的なリファクタリング案（参考）

もし将来的にリファクタリングが必要になった場合、以下のような分離が考えられます：

```
starship-sim/
├── index.html              # エントリーポイント
├── src/
│   ├── core/
│   │   ├── GameEngine.js   # ゲームループ管理
│   │   └── Renderer.js     # 描画エンジン
│   ├── physics/
│   │   ├── PhysicsSystem.js  # 物理演算
│   │   └── CollisionSystem.js # 衝突判定
│   ├── entities/
│   │   ├── Starship.js
│   │   ├── Bullet.js
│   │   └── Particle.js
│   ├── components/
│   │   ├── Hull.js
│   │   ├── Engine.js
│   │   └── Weapon.js
│   └── input/
│       └── InputManager.js
└── README.md
```

ただし、**現時点ではこの分離は不要**です。

## まとめ

Starship Sim は、以下の理由から**現在のアーキテクチャを維持**します：

1. 外部ライブラリ非使用により、シンプルさと透明性を保持
2. すでに洗練されたクラス設計とコンポーネントパターンを実装済み
3. 実験的プロジェクトとしての性質に最適化された構造
4. 1,550行は管理可能な規模であり、過剰な抽象化は不要

このアーキテクチャは、**教育的価値、配布の容易さ、開発のシンプルさ**のバランスが取れた、このプロジェクトに最適な選択です。

---

**策定日**: 2026-02-03
**最終更新**: 2026-02-03
